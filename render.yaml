services:
  - type: web
    name: sudokupad-party
    runtime: node
    buildCommand: echo "No build step needed"
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeployTrigger: commit
